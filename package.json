{
  "name": "gestion-eventos",
  "version": "1.0.0",
  "description": "SPA para gestión de eventos con json-server",
  "main": "index.js",
  "scripts": {
  "server": "json-server --watch ./database.json --port 3001",
  "dev": "npm run server"
},
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}